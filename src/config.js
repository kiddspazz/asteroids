const size = 600;
// this is the number that makes them feel like the right size
const asteroidDivisor = 14;

const config = {
  width: size,
  height: size,
  asteroidStartSize: size / asteroidDivisor,
};

export default config;
